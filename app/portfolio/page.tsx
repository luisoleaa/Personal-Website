import DefaultNavPage from "@/components/common/DefaultNavPage";

export default function PortfolioPage() {
  return (
    <DefaultNavPage
      eyebrow="Portfolio"
      title="Selected work"
      description="This page is ready for project cards, case studies, links, screenshots, and the technical details behind what you have built."
    />
  );
}
